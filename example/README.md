# Example

Demo app for [`swipe_reveal_card`](https://pub.dev/packages/swipe_reveal_card).

```bash
cd example
flutter run
```

Swipe any card left to reveal **Edit**, **Archive**, and **Delete** actions.
